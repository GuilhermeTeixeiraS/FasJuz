import FormContrate from "../../components/FormContrate";
import './index.css'

function Contrate({ page, setPage }) {

  return (
      <main className="mainContrate">
      <FormContrate />
      </main>
  );
}

export default Contrate;
